import * as Styled from "./Header.styled";

export const Header = () => {
  return (
    <Styled.Container>
      <Styled.Title>Search in the Skill Tree</Styled.Title>
    </Styled.Container>
  );
};
